# 饥荒联机版手柄震动兼容

[English](README.en.md)

让游戏中能被客户端观察到的原版震动事件，通过已可工作的手柄输出接口恢复反馈。纯客户端安装，支持局内调整强度、时长和手柄响应方式。

当前版本为 1.5.0，安装包见[发布页面](https://github.com/Wuty-zju/dst-haptics-compatibility/releases)。

## 安装

下载发布包，解压至游戏的 `mods` 文件夹，确认文件位于：

`Don't Starve Together/mods/dst_haptics_compat/modinfo.lua`

在游戏主菜单的模组页面启用本模组，并开启原版“控制器震动”。服务器和其他玩家无需安装。使用创意工坊版本时，避免同时开启本地副本。

## 在世界中调整

按 F8 打开配置；手柄用户可从暂停菜单使用底部提示的震动设置快捷键。保存后约半秒生效。

强度和时长分别提供 **0%–200%、每档 5%** 的调节，默认均为 100%。可以统一调整，也可以分别调整以下类型：

| 类型 | 对应反馈 |
|---|---|
| 工具 | 原版定义的砍树、采矿、挖地等工具事件 |
| 战斗 | 挥动武器、材质命中及匹配的战斗事件 |
| 危险 | 原版危险分类，例如受击、温度等 |
| 玩家交互 | 原版玩家分类及未指定分类的回退 |
| Boss | 原版 Boss 分类中的脚步、技能等 |
| 环境 | 原版环境分类 |
| 界面 | UI、HUD 及已确认的本地界面事件 |
| 持续循环 | 有名称并受声音生命周期管理的循环 |

原版分类与工具、战斗等语义类型是独立倍率。例如 Boss 战斗反馈使用“总体 × Boss × 战斗”；Boss 循环使用“总体 × Boss × 循环”。每档均为 100% 时保持兼容层的原版基准包络；任何适用轴设为 0% 即不输出。高倍率组合最终受到马达输出上限限制。

单次时长会同时缩放包络内各段震动的宽度与间隔。循环时长调整每次脉冲宽度，原声音仍决定开始与结束；不会因为设置为 200% 就在声音停止后继续震。

**100% 的含义**：未额外缩放的原版兼容基准。核心事件依据本机原版波形测量，其他事件使用语义包络近似。当前游戏公开接口不能保证绝对马达强度、持续时间与原生波形逐采样相同。

## 手柄与响应方式

支持 Xbox/XInput、DualShock 4、PS5 Controller 和 DualSense 的游戏设备路径。所有手柄遵循同一事件逻辑。

“手柄专用适配”默认开启，在输出端应用设备响应曲线，保留多段包络的强弱变化；关闭后回退通用响应。Steam Input 将索尼手柄识别为虚拟 Xbox 时，可手动指定 DS4 或 DS5 模式。还可以选择原版细节、柔和或强力响应。

DS4、DS5 的实际震动需要游戏或 Steam Input 提供对应输出通道。纯 Lua 模组不能直接发送 USB、蓝牙马达报告，也不提供 DualSense 自适应扳机或逐采样高频触觉。[手柄验收矩阵](docs/HARDWARE_MATRIX.md)区分代码覆盖和实际硬件测试。

配置中的弱、中、强测试脉冲由用户主动触发，可用于检查输出与体感层次；它不参与玩法判断。

## 震动如何触发

启动时读取当前游戏自带的 `haptics.lua`，以其中的事件与字段为依据。客户端真实播放的声音从声音接口进入兼容层；服务器端工具命中、近战材质命中和本地受击等缺口，使用服务器复制结果确认后桥接。原版同时定义挥动和命中时，两者分别保留。

玩家归属、原版分类、相对强度、声音参数和具名循环生命周期参与输出判断。世界事件按音频监听器位置衰减，本地界面事件不走世界距离。

兼容模式抑制原生震动输出，避免未来游戏修复后双震。“原生”模式恢复游戏自身路径；“仅诊断”模式关闭两条马达输出，只记录可观察事件。

## 已知边界

- 核心事件使用原版资源测量得到的多段包络；其余事件按语义近似。不能宣称与 Klei 原始波形完全相同。
- 完全由引擎复制且没有客户端 Lua 信号的声音，无法通用截获；部分 Boss 与环境事件受此限制。
- 游戏新增事件会自动进入索引，但实际反馈仍取决于客户端是否能观察到触发。
- 自动测试能验证比例、过滤和生命周期，不能代替真实手柄的 USB、蓝牙及体感验收。

## 排查问题

先检查原版“控制器震动”、本模组“震动适配”、输出模式，以及相关强度和时长是否为 0%。需要记录问题时开启调试日志，复现一次后查看：

- 正式分支：`Documents/Klei/DoNotStarveTogether/client_log.txt`
- 测试分支：`Documents/Klei/DoNotStarveTogetherBetaBranch/client_log.txt`

提交问题时附上游戏版本、手柄型号、连接方式、Steam Input 状态、相关配置和日志中的事件记录。

如果 Klei 修复了原生震动，将“震动适配”关闭，或把输出模式切换为“原生”。

## 开发与维护

[1.5.0 计划](docs/PLAN-1.5.0.md) · [开发记录](docs/notes/1.5.0-progress.md) · [架构](docs/ARCHITECTURE.md) · [测试](docs/TESTING.md) · [创意工坊分发](docs/STEAM_WORKSHOP.md)

仓库不分发 Klei 原版脚本和原始波形资源。图标为原创线条设计，可编辑源文件位于 `assets/icon.svg`。
